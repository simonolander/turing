{
  "private": true,
  "devDependencies": {
    "purescript": "0.14.0",
    "spago": "0.19.1"
  },
  "scripts": {
    "build": "spago build",
    "test": "spago test",
    "bundle": "rm -rf dist && spago bundle-app --to index.js"
  }
}
